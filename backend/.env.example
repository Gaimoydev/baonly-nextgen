# 配置原则：只有【极敏感】或【运行时必需】的项放这里。
# 其余配置进数据库 AppConfig 表，后台可改。

NODE_ENV=development
PORT=3000

DATABASE_URL="postgresql://USER:PASSWORD@127.0.0.1:5432/baonly?schema=public"
REDIS_URL="redis://127.0.0.1:6379"

# 以下为凭据类，按需填写（同样不入库）
# CPP_ACCOUNT=
# CPP_PASSWORD=
# WXPUSHER_APP_TOKEN=
# SCDN_TOKEN=
